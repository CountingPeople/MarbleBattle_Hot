using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class SetPerfabItem :Editor
{
    //[MenuItem("Assets/AddPerfabConfig")]
    //private static void AddPerfabConfig()
    //{
    //    PerfabConfig perfabConfig = AssetDatabase.LoadAssetAtPath<PerfabConfig>(GameConfig.perfabConfigPath);
    //    var allSelectGuid = Selection.assetGUIDs;
    //    var all = Selection.objects;
    //    foreach (var guid in allSelectGuid)
    //    {
    //        PerfabItemData config = new PerfabItemData(guid);
    //        perfabConfig.AddPerfab(config);
    //    }
    //}
}
